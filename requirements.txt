# ==============================================================================
# Core Dependencies - Required for application to run
# ==============================================================================

# Web framework
fastapi==0.110.0

# ASGI server with standard extras (includes uvloop, httptools for performance)
uvicorn[standard]==0.27.1

# Database ORM and migrations
SQLAlchemy==2.0.45
alembic==1.17.2

# Data validation and settings management
pydantic==2.10.3
python-dotenv==1.0.1

# File handling
python-multipart==0.0.18  # Required for FastAPI file uploads (updated for CVE fix)
openpyxl==3.1.3          # Excel file processing (.xlsx)

# ==============================================================================
# Optional Dependencies - Database Adapters
# ==============================================================================
# Uncomment the database adapter you need:

# PostgreSQL adapter (currently not used, SQLite is default)
# psycopg2-binary==2.9.11

# ==============================================================================
# Development & Testing Dependencies
# ==============================================================================
# Note: Install these separately for development with:
# pip install -r requirements-dev.txt

# httpx>=0.24.0       # Required for FastAPI TestClient
# pytest>=7.4.0       # Test framework
# pytest-cov>=4.1.0   # Coverage reports